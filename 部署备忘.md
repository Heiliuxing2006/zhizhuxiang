# 智猪侠网站 · 改代码到上线 备忘

一句话原理：**本机改代码 → 推到 GitHub → Render 自动部署线上**。

```
改文件  →  git commit（存快照）  →  git push（传到 GitHub）  →  Render 自动更新线上网站
```

---

## 日常上线流程（改完代码后照着跑）

在终端里，先进入项目目录：

```bash
cd ~/Documents/智猪乡盟网站
```

然后三步：

```bash
# 1. 看看改了哪些文件（绿/红列表，确认没问题）
git status

# 2. 把所有改动存成一次快照（引号里写这次改了什么）
git add -A
git commit -m "这次改动的简短说明"

# 3. 推到 GitHub（推完 Render 会自动部署）
git push origin main
```

推送成功后，去 Render 控制台看部署进度，变成 **Live** 就上线了。
打开线上网站按 **Cmd + Shift + R** 强制刷新（避开旧缓存）。

---

## 本机 Docker 自测（可选，上线前先看效果）

```bash
cd ~/Documents/智猪乡盟网站
docker build -t zhu-tuo-suo .
docker rm -f zhuxia 2>/dev/null; docker run -d --name zhuxia -p 3002:3001 zhu-tuo-suo
```

浏览器打开 **http://localhost:3002** 看效果。
（端口 3002 被占就换 3003：把 `-p 3002:3001` 改成 `-p 3003:3001`，访问 3003）

---

## 常见卡点

**报错 `index.lock: File exists`**
有残留锁文件，删掉再重跑：
```bash
rm -f .git/index.lock
```

**push 报错，提示要先 pull**
GitHub 上有本机没有的更新，先拉下来再推：
```bash
git pull origin main
git push origin main
```

**想撤销还没 commit 的改动**（把某个文件改回上次提交的样子）：
```bash
git checkout -- 文件名
```

---

## 名词速记

| 词 | 是什么 |
|---|---|
| git | 本机的代码存档工具，命令行用，不用手动启动 |
| GitHub | 云端备份+共享中心，存 git 仓库的副本 |
| commit | 存一次快照（带说明） |
| push | 本机 → GitHub 上传 |
| pull | GitHub → 本机 下载 |
| main | 主分支（线上用的就是它） |
| Render | 部署平台，盯着 GitHub 的 main 分支自动上线 |

---

## 待办（有空再弄，不急）

- 后台密码 `zhizhuxiang2026` 明文写在 server.js 里，代码已公开在 GitHub。
  建议改成 Render 环境变量，别写死在代码里。
- 旧的 `.xlsx` 看地统计表历史还在 git 仓库里。彻底移除：
  `git rm --cached "益阳市100套GROW项目看地信息统计表.xlsx"` 然后 commit。
